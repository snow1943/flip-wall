# Flip Wall

当前建议从 `v13_refactor/` 开始使用和开发。

## 目录说明
- `v13_refactor/` 当前模块化版本（推荐）
- `原始草稿/` 历史草稿与探索版本（归档参考）
- `output/` 截图和测试产物
- `vendor/` 第三方依赖（Three.js 等）

## 运行
1. `cd /Users/hiram/vibecoding/flip_wall`
2. `python3 -m http.server 5173`
3. 访问 `http://localhost:5173/v13_refactor/index.html`
